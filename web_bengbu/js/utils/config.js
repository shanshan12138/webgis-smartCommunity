/**
 * 配置信息
 */

//本机服务地址
var ipPort = "http://localhost:8080/dsjh/";

//车小丫执法仪服务地址
// var cxyZfyPort = "http://localhost:8090/super_community_chexiaoya/";

//屏幕宽高
var screenWidth = document.documentElement.clientWidth || document.body.clientWidth;
var screenHeight = document.documentElement.clientHeight || document.body.clientHeight;

//服务器消息
var MSG_LOGIN_TIMEOUT = "登录过期!";
// var CORS_ORIGIN_ALLOW_ALL = true; //允许所有源访问（如果不需要允许全部，可以设置CORS_ORIGIN_WHITELIST=（）参数，将需要访问的域名添加即可）
// var CORS_ALLOW_CREDENTIALS = true;  //是否允许携带cookie